{
  "name": "gisthub",
  "version": "0.0.1",
  "private": true,
  "description": "GistHub - A VS Code extension and Web Dashboard for managing GitHub/Gitee Gists",
  "scripts": {
    "dev:web": "pnpm --filter gisthub-web dev",
    "build:web": "pnpm --filter gisthub-web build",
    "build:extension": "pnpm --filter gisthub-extension compile",
    "build": "pnpm run build:web && pnpm run build:extension"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}